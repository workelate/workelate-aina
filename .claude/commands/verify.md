Run the deterministic verify gate and report results.

1. Ensure the dev server is running on http://localhost:4310
   (`npm run dev` in background if not).
2. Run `npm run verify`.
3. Paste the full check table in the response.
4. If any check fails, fix it and re-run before doing anything else.
   Do not move to the next section on red.
