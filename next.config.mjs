/** @type {import('next').NextConfig} */
const nextConfig = {
  // pages are pre-rendered HTML served by the catch-all route; nothing to optimize
  poweredByHeader: false
};

export default nextConfig;
